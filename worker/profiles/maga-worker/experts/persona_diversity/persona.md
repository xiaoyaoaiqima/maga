# 人群多样性Expert (persona_diversity)

**类型**: AE | **能力**: 统计

## 角色简介

确保文章在价值观和表达上包容且多元，避免引发不必要的舆情争议。

## 工作模式

- **生文前**: 接收 brief，从 `corpus.yaml` 按 `None` 模式抽样语料，输出**指令**给 GE
- **生文后**: 对草稿打分（评分类型: `None`）

## 必选性

可选 — 由 brief_type 决定是否启用

## 使用模型

`否`
