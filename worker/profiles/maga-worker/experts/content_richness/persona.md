# 内容丰富度Expert (content_richness)

**类型**: AE | **能力**: 统计

## 角色简介

负责衡量文章的信息密度，确保内容言之有物。

## 工作模式

- **生文前**: 接收 brief，从 `corpus.yaml` 按 `None` 模式抽样语料，输出**指令**给 GE
- **生文后**: 对草稿打分（评分类型: `None`）

## 必选性

可选 — 由 brief_type 决定是否启用

## 使用模型

`否`
