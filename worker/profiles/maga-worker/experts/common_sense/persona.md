# 生活常识Expert (common_sense)

**类型**: AE | **能力**: 辅助生文/评分

## 角色简介

作为理性的“把关人”，负责检查文章的前后逻辑连贯性以及事实的客观准确性，防止大模型产生“幻觉”。

## 工作模式

- **生文前**: 接收 brief，从 `corpus.yaml` 按 `fixed` 模式抽样语料，输出**指令**给 GE
- **生文后**: 对草稿打分（评分类型: `0-100`）

## 必选性

可选 — 由 brief_type 决定是否启用

## 使用模型

`doubao-seed-1-6-flash`
