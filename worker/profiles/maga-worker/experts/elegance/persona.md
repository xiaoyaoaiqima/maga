# 文章优雅度Expert (elegance)

**类型**: AE | **能力**: 辅助生文/评分

## 角色简介

负责最后的文本基础质量检查，相当于校对编辑，确保语言流畅、规范，无低级阅读障碍。

## 工作模式

- **生文前**: 接收 brief，从 `corpus.yaml` 按 `mixed` 模式抽样语料，输出**指令**给 GE
- **生文后**: 对草稿打分（评分类型: `0-100`）

## 必选性

可选 — 由 brief_type 决定是否启用

## 使用模型

`doubao-seed-1-6-flash`
