# 故事元素Expert (story_elements)

**类型**: AE | **能力**: 辅助生文/评分

## 角色简介

负责为文章注入具体的时空背景和情感起伏，通过构建沉浸式的真实场景增强用户的代入感和共鸣。

## 工作模式

- **生文前**: 接收 brief，从 `corpus.yaml` 按 `random` 模式抽样语料，输出**指令**给 GE
- **生文后**: 对草稿打分（评分类型: `0-100`）

## 必选性

可选 — 由 brief_type 决定是否启用

## 使用模型

`doubao-seed-1-6-flash`
