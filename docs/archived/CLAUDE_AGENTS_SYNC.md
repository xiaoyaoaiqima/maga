# CLAUDE.md 和 AGENTS.md 同步记录

> **更新时间**: 2025-01-08
> **目的**: 记录两个文档之间的差异和同步情况

---

## 已修正的矛盾点

### 1. ✅ 服务端口号统一

**修正前**：
- CLAUDE.md: orchestrator (5101), generation-experts (5102), user-memory (5103), ag (5104), keyword-corpus (5105)
- AGENTS.md: 示例代码中用 5100

**修正后**：
- 所有服务统一为 **5100**
- 理由：根据 `values.yaml`，所有服务都配置为 `port: 5100`
- K8s 通过 Service 名称进行服务发现，不需要不同端口

**影响文件**：
- `CLAUDE.md` - 服务端口表、服务职责部分、API 文档访问、健康检查

---

### 2. ✅ 数据库连接配置更新

**修正前**：
- CLAUDE.md: `raap/raap123456@localhost:3306/raap_db`
- 这是过时的本地开发配置

**修正后**：
- 使用 `values.yaml` 中的实际配置
- MySQL:
  - Host: `10.88.88.62`（外部数据库）
  - Database: `raap_db`
  - User: `rs_raap_dev`
  - Password: `^V%NC-6K!Dp6GsH`
- Redis:
  - Password: `redis123456`
  - 部署在 K8s `raap-dev` namespace
- 添加说明：数据库查询可使用 MCP 工具查看

**影响文件**：
- `CLAUDE.md` - 数据库连接部分

---

### 3. ✅ 补充镜像管理规范

**新增内容**：
- 镜像来源规范（从 AGENTS.md 同步）
- 只允许从 TCR（腾讯云容器镜像服务）拉取
- 缺失镜像自动从 `m.daocloud.io` 同步到 TCR
- 使用 `--deploy` 参数自动处理镜像同步

**影响文件**：
- `CLAUDE.md` - 新增"镜像管理"章节

---

### 4. ✅ 补充 PR 规范

**新增内容**：
- PR 描述必须包含的内容（变更范围、验证方式、API/配置/端口变更、截图）
- PR 标题格式
- 完整的 PR 示例模板

**影响文件**：
- `CLAUDE.md` - Commit Message 章节扩展为"Commit Message 和 PR 规范"

---

### 5. ✅ 强化前端 Linter 规范

**修正前**：
- 提到了 linter，但不突出

**修正后**：
- 单独列出"🚨 Linter 规范"
- 强调"修改后必须检查并修复 linter 错误"
- 强调"提交代码前确保没有 linter 警告"

**影响文件**：
- `CLAUDE.md` - 前端开发注意事项部分

---

## 文档特点对比

### AGENTS.md 的特点

- **简洁规范**：强调强制规则（🚨）和建议（⚠️）
- **快速参考**：常用命令速查表
- **代码示例**：直接展示正确/错误用法
- **全面覆盖**：包含前端、后端、部署、提交规范

### CLAUDE.md 的特点

- **详细说明**：包含架构理解、调试场景、故障排查
- **操作指南**：快速启动、常用命令、调试流程
- **可视化**：请求链路图、架构说明
- **实践导向**：调试检查清单、场景示例

---

## 两个文档的定位

### AGENTS.md - 规范手册
- **目标读者**：所有开发者
- **用途**：快速查阅规范和规则
- **特点**：简洁、权威、规则明确

### CLAUDE.md - 操作指南
- **目标读者**：Claude Code AI 助手
- **用途**：理解项目架构、指导开发调试
- **特点**：详细、全面、场景丰富

---

## 后续维护建议

### 同步原则

1. **规范变更**：
   - 先更新 AGENTS.md（规范手册）
   - 再同步到 CLAUDE.md（操作指南）

2. **配置变更**：
   - 更新 `values.yaml` 后
   - 同步更新 CLAUDE.md 的数据库配置部分

3. **新功能/新规范**：
   - 同时更新两个文档
   - AGENTS.md 简洁描述
   - CLAUDE.md 详细说明

### 检查清单

定期检查两个文档的一致性：
- [ ] 服务端口号一致
- [ ] 数据库配置与 values.yaml 一致
- [ ] Commit 规范一致
- [ ] 关键强制规则一致

---

## 待同步项

### 从 AGENTS.md 同步到 CLAUDE.md ✅ 已完成

- [x] 服务端口号统一为 5100
- [x] 数据库配置更新为 values.yaml 的实际配置
- [x] 镜像管理规范
- [x] PR 规范
- [x] 前端 Linter 规范强化
- [x] MCP 工具查看数据库说明

### 从 CLAUDE.md 同步到 AGENTS.md

- [ ] 请求链路图解（如果需要）
- [ ] 调试日志查看规范（如果需要）
- [ ] 前端端口检查原则（如果需要）

---

**维护建议**：每季度进行一次全面同步检查
