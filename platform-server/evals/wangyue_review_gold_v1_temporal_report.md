# 旺玥时间逻辑审核评测 v1

## 结论

这 15 条已于 2026-07-15 完成人工确认，冻结为时间逻辑首批回归基线。后续样本追加，不直接改写本批金标。

已建立 `wangyue_review_gold_v1 / temporal_logic_v1`，当前包含 15 条人工金标：

- `block`：6 条
- `pass`：7 条
- `watch`：2 条
- `WYT-005` 按人工标准接受 `pass/watch`

短时间逻辑 Judge 已完成离线候选验证，但尚未接入生产审核链路。

## 目标问题

历史失败样本 `WYT-001`：

- 前文：“这段时间他好像特别容易中招，三天两头喉咙不舒服”
- 后文：“回看这段时间状态倒是挺稳”

两个相反的持续状态落在同一个时间窗口，中间没有“以前、前阵子、后来、换后”等阶段转换。人工金标为：

```json
{"label":"block","issue_code":"same_period_state_contradiction"}
```

## 当前机器基线

这里只统计现有时间专项检测器，不混入产品缺失、年龄、公共疾病等其他 guard。

| 指标 | 当前时间检测器 |
|---|---:|
| 红线召回率 | 16.7% |
| 非红线误拦率 | 0% |
| 可输出 watch | 否 |

当前检测器只稳定抓到“现在正是流感季”这类显式发布时间冲突；以下能力缺失：

- 同一时间窗口状态矛盾
- 单次小状况后立即用旺玥补救
- 刚换产品就承接长期效果
- 局部偶发状态与整体稳定之间的 watch 边界

## 候选 Judge

候选服务：`WangyueTemporalLogicJudgeService`

模型只输出：

```json
{"label":"pass|watch|block","issue_code":"枚举值","evidence":"原文证据"}
```

它不审核产品事实、违禁词、人味或种草，不提供改写建议，也不输出 `rewrite_required / severity / business_usability_tier / 主观评分`。

## 校准记录

| 轮次 | 只改的变量 | 一致率 | 红线召回 | 非红线误拦 |
|---|---|---:|---:|---:|
| v1 | 第一版短 rubric | 73.3% | 66.7% | 0% |
| v1.1 | 区分“高频持续负面”与“偶发局部状态” | 80.0% | 83.3% | 0% |
| v1.2 | 修正 `pass/watch` 金标边界；格式失败重试 | 100% | 100% | 0% |
| v1.3 | 明确 issue code 枚举 | 100% | 100% | 0% |

v1.3 运行数据：

- 样本数：15
- 并发：5
- 总耗时：7045 ms
- 单条平均调用耗时：2151 ms
- 总 tokens：8015
- 格式重试：0

## 当前边界

- 这 15 条只覆盖时间逻辑，不代表旺玥总审核已完成校准。
- 当前生产长 Prompt 保持不变。
- 当前生产程序聚合逻辑保持不变。
- 候选 Judge 暂不参与入池、拦截或改写。
- 下一步应先补更多历史真实失败和容易误杀的时间样本，再做重复运行稳定性测试。
