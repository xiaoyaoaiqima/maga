# A2 舆情改善评论审核规则

## 审核顺序

`生成评论 -> machine audit -> LLM review -> operator judgment`

- machine audit 负责违禁词、硬禁词、长度、重复度和确定性的分类锚点。
- 机器未通过项标记为 `machine_blocked / not_run`，不送给 LLM reviewer，也不计入 LLM 可用档位。
- LLM review 只判断分类贴合、事实支持、评论质感和修改成本，不重复审核机器硬规则。
- LLM reviewer 必须结合整句语义以及当前规则的 `focus + examples` 判断，不得维护逐词黑名单或白名单，也不得因为单个词语直接降档。
- 新的词面误判优先修正语义判断原则或所属机器规则，不在 LLM prompt 中逐个追加词语例外。
- 没有真实运营确认时，`operator judgment` 必须显示为 `not run`。

## 迭代记录

- 2026-07-29：A2“有货”评论删除纯观察、劝少囤和低情绪中立策略。每条必须明确传达有货，并体现评论者本人积极的购买、补一罐、已经补到、继续回购或继续选择意愿；允许自然出现第一人称购买动作，但继续禁止催促他人购买、导购式号召和渠道库存承诺。“不用囤、按需再买、不用着急、还行”等不能作为评论主结论。业务专家连续保留“家里快喝完补到、亲友告知有货、商超货架看到”等具体微场景，因此微场景继续由模型自由探索，单个编造感 badcase 定点调整，不升级为全局禁令。
- 2026-07-20：发布 `a2_sentiment_comment_activity v82`。新增两条确定性边界：所有新生成的 A2 评论不描述罐身颜色；“物流码、二维码”在新生成链路统一规范为“罐底码”，正文也可自然简称“码”，三方报告统一写“三方质检报告”。历史交付文件不回改，review replay 也不会仅因这次称谓升级改写旧正文。业务提供的长场景清单拆回 `a2_direct_45..49` 的内容方向与表达路径：批批检层保留每批可查、选奶关注、买回查看、朋友/导购提醒和长期使用后多确认等经历；罐底码与三方质检报告只在扫码规则承载；“对比不同批次发现报告不同”不直接写成报告内容差异，只保留“不同罐分别对应各自报告”。每条评论只落一个核心信息点，避免把检测信息、报告、感受和便利性同时堆进一句。
- 2026-07-18：有货直给由“简单报喜 / 已经买到 / 准备购买”三条合并为两条：`a2_direct_01 / 有货-直给-提产品` 与 `a2_direct_43 / 有货-直给-不提产品`，删除 `a2_direct_44`。两条共用极简生文指令、内容方向、20字写法和消极供货词边界，只通过内容素材区分是否出现a2/a2至初；允许评论自然出现下单、囤货等个人购买动作，但继续拦截调货、留货、全国补货等渠道或库存承诺。
- 2026-07-17：平台口语表达“果断回购、反手点赞、绝了、真的爱了、圈粉、点赞”等本身可用，不得因为看起来情绪强或像平台热词就作为单条评论的人工风险词过滤。此类表达只在批次中高频复用、形成同质化收口时由相似度和短语聚类处理；单条审核只继续拦正式违禁词、未确认业务事实和“绝对安全、闭眼喝、保证”等确定性结论。
- 2026-07-17：四类评论生产统一将生文指令升级为“生成一条小红书母婴社区真实用户评论，口语化，有活人感。句式结构多样一些，生成同质化的内容是原罪。”，发布 `a2_sentiment_comment_activity v75`，并新增 `a2_direct_48 / 罐底扫码直给`、`a2_direct_49 / 三方质检报告直给`。正式交付四类各100条时，不直接使用原始批次全量，而是在机器通过候选上继续排除正式违禁词、未确认业务事实和确定性安全结论，再用字符2-gram贪心选择控制任意入选对相似度低于0.6；最终400条完全重复为0、相似度告警为0、入选风险项为0。
- 2026-07-17：从 `a2_UGC评论话术库_20260716_查重修订版.xlsx` 的“批批检、批次报告、检测透明”列中只选6条方向demo，新增 `a2_direct_45 / 批批检-直给认可`、`a2_direct_46 / 批批检-批次报告直给`、`a2_direct_47 / 批批检-检测透明直给`，发布 `a2_sentiment_comment_activity v74`。三条均使用 `comment_prompt_bundle`，demo保留在母池但不进入生成Prompt；旧的 `a2_direct_35` 继续承担中立观望，不与直给任务混用。
- 2026-07-17：`有货`方向的完整短评论放宽为去除首尾标点后至少5个字符且包含供货表达即可；供货表达包含“到货、来货、有货、能买、买到、能拍、补货、上架、到了”。一般有货规则不强制出现 `a2/至初`，但2026-07-18拆分后的 `有货-直给-提产品 / 不提产品` 按各自规则确定性校验产品名。例如“a2到了！终于等到了！”和“终于等到了！”应通过，少于5字符或只有“太好了”等无供货语义短句仍拦截。
- 2026-07-17：A2评论改为业务规则自带 `comment_prompt_bundle`，每条内容方向把 `生文指令 / 内容方向 / 活动素材 / 写法 / 注意` 作为一组保存和渲染；命中该协议时不加载全局评论关键词、不抽随机语气槽、不渲染业务示例。`有货-直给到货情绪` 拆为 `简单报喜 / 已经买到 / 准备购买` 三条规则，购买状态和活动素材互不混用。单一窄方向内部仍可能出现重复，后续应由批次选择层处理，不再往Prompt里堆同义改写要求。
- 2026-07-17：审核 `a2_UGC评论话术库_20260716_查重修订版.xlsx` 后，只吸收少量有具体生活入口的 demo，不导入整批 400 条组合句。`集罐换礼 / 抽奖活动` 内容方向补充“刚知道、已经在攒、准备兑换、顺手参加”等自然入口。正式活动素材确认：集罐礼品为扭扭车、自行车、奶粉、婴儿推车；溯源抽奖礼品为新西兰溯源、a2&小马宝莉黄金手串、宝宝夏凉被、a2营养全家礼、积分。具体礼品 demo 必须保留并与所属活动机制对齐，不能因为当前活动素材记录不完整就自行删除。提示词里的前缀、结尾、句式骨架、语义和历史查重应落在批次质检层，不回填到单条 Bundle Prompt。
- 2026-07-16：`a2_direct_01 / 有货-直给到货情绪` 不再保留“平常地说一句”的低情绪入口；内容方向明确使用开心、惊喜、松口气或小激动，但禁止催抢喊话。20条实跑显示情绪方向有效，同时暴露 `comment_micro_batch_check_reply` 会把扫码、批批检、未检出带入有货方向，后续需收紧格式路由。
- 2026-07-16：评论 prompt 的 `任务 / 生评论指令` 合并为单一 `生文指令` 渲染层；业务规则可拆成 `内容方向 / 活动素材`。首个正式试点为 `a2_direct_01 / 有货-直给到货情绪`：内容方向只描述到货后的即时反应，a2 到货、来货、买到新货等具体信息放活动素材，真人示例保留原始口语毛边。
- 2026-07-16：评论身份和基础输出要求统一由系统生文指令提供；A2 评论 prompt 不再渲染 `scenario_post_context`，即使资产仍保留该字段，也直接从任务层开始，避免重复的“你正在小红书母婴评论区”固定入口。
- 2026-07-16：有货评论里的自然购买数量（如两罐、两盒、一箱、两箱）本身可用，不得因为模型补了购买数量直接过滤；审核重点仍是库存承诺、催抢、跨分类信息和不合理购买场景。
- 2026-07-16：品牌名和产品名统一写小写 `a2`；只有营养概念明确写成 `A2蛋白` 时允许大写。批量交付需在最终 CSV 层复验 `A2(?!蛋白)` 为零命中。
- 2026-07-11：模型测试中通过机器审核和 LLM review 的评论也属于候选资产，必须保留模型、分类、batch_id 和审核档位后汇总入池；Direct、LightFix 与 operator judgment 分开保存，不得因测试用途直接丢弃。
- 2026-07-11：LLM reviewer 移除具体违禁词、允许词及词面例外，只保留业务可用性判断；机器审核继续作为前置门禁。
